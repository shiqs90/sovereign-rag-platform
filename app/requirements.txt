# Serving dependencies only. The corpus extractor's deps (pymupdf) and the eval's
# (pyyaml) live in pyproject.toml and never enter the image.
fastapi>=0.141.1
uvicorn>=0.52.1
httpx>=0.28.1
qdrant-client>=1.19.0
