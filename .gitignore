.DS_Store
CLAUDE.md

# Personal build notes — carries JD/interview framing that the committed docs deliberately don't
P10-SUMMARY.md

# Python — uv.lock IS committed (it pins exact versions; that is the point)
.venv/
__pycache__/
*.pyc

# Terraform — provider binaries (200MB+), local state, and anything with secrets.
# .terraform.lock.hcl is deliberately NOT ignored: it pins provider versions.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log

# uv-files
uv.lock
docs/